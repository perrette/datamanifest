# Python API reference

The public surface of the `datamanifest` package, generated from the
docstrings. The narrative guide is [Using it from code](api.md).

## Module-level functions

::: datamanifest
    options:
      members:
        - add
        - register_dataset
        - download_dataset
        - download_datasets
        - load_dataset
        - get_dataset_path
        - delete_dataset
        - resolve_db
        - get_default_database
        - validate_loader
        - validate_loaders

## The `Database` class

::: datamanifest.Database

## Dataset entries

::: datamanifest.DatasetEntry

## Caching

::: datamanifest.cache.cached
